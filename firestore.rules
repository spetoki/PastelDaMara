rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra de desenvolvimento: permite acesso total.
    // ATENÇÃO: Isso deve ser alterado para um ambiente de produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
