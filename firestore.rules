rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Esta regra permite acesso aberto para desenvolvimento.
    // AVISO: Inseguro para produção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
